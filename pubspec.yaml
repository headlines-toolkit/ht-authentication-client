name: ht_authentication_client
description: Authentication client for the Headlines Toolkit, supporting email/password, Google Sign-In, anonymous login, user session management, and account deletion.
publish_to: none
repository: https://github.com/headlines-toolkit/ht-authentication-client

environment:
  sdk: ^3.7.2
  
dependencies:
  equatable: ^2.0.7
  json_annotation: ^4.9.0
  uuid: ^4.5.1
  
dev_dependencies:
  build_runner: ^2.4.15
  json_serializable: ^6.9.4
  mocktail: ^1.0.4
  test: ^1.24.0
  very_good_analysis: ^7.0.0
